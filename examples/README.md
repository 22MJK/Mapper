# Examples

This directory contains small, self-contained hardware topology, workload, and
LLM config examples used by the README commands.

The C++ parsers may still accept some legacy aliases for compatibility, but new
examples should follow the field names already used by the JSON files in this
directory.
